type Query {
  "It's login query demo description"
  login(
    "username It's name of current user"
    username: String!,
    "password It's password"
    password: String!
  ): SignInResponse
  logout: Boolean
}
"""
demo
multi-line
comment
"""
type SignInResponse {
  accessToken: String
  expiresIn: Int
  id: Int
}

